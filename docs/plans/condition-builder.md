<!--
Generated by the condition-builder-plan workflow (run wf_150acec2-a96) on 2026-06-09.
15 agents: 5 grounding readers, 4 persona lenses, 1 designer, 4 adversarial critics, 1 finalizer.
Planner-locked, developer-ready. Supersedes docs/proposals/condition-builder.md.
-->

# Plan: Survey condition builder — broader choices + chainable + grouped expressions

**Version:** v0.3 — 2026-06-09
**Status:** PLANNER-LOCKED. Ready to hand to a developer session.
**Supersedes:** `docs/proposals/condition-builder.md` (drafted 2026-06-09, "not yet planner-locked") and plan v0.1 (2026-06-09). This plan corrects the proposal's factual defects AND the empirically-verified round-trip defects the adversarial reviewers found in v0.1 (serializer whitespace normalization, the `parseRelevant` short-circuit on mixed combinators, `setExtra` delete-on-empty, the n===1 op-dropdown contradiction, the `selected()` prose gap, and line-anchor errors). Where this plan and the proposal/v0.1 disagree, **this plan wins**.

---

## 0. Planner decisions on the open questions (§9 of proposal)

| # | Question | Decision | Rationale |
|---|----------|----------|-----------|
| 1 | Phasing: one PR (~650 LOC) or two slices? | **Two slices.** Slice 1 = choices fix (read-only, ~80 LOC). Slice 2 = flat chaining + group affordance (~570 LOC). | Choices fix is fully independent, touches zero XLSForm bytes, and is the single highest-value item per Bhishan. Shipping it standalone delivers value this week and de-risks the parser work. Lorena/Developer both require Slice 1 to contain **zero** `GroupedExpression` code so the 85 shared tests + 5 consumers stay green standalone. |
| 2 | Group-affordance sequencing: gate behind choices+flat-chaining, or all-three together? | **Group affordance lands in Slice 2 (with flat chaining), but as a strictly additive parser entry point — never a widening of the existing `parseRelevant` signature.** Within Slice 2, sequence the parser+serializer+round-trip tests FIRST (commit A), then the flat-chaining UI (commit B), then the group affordance UI (commit C). | The user locked the group affordance into the feature (not deferrable). The breaking-change risk is entirely in *how* the parser is extended, not *whether*. By adding `parseRelevantGrouped(): AnyParsed` as a NEW export and leaving `parseRelevant(): ParsedExpression` byte-identical, the union becomes genuinely additive: the 5 existing consumers (RelevantRuleBuilder.tsx:37/38/42, CalculationBuilder.tsx:54/173/184/208, DecisionsView.tsx:438, shared/calculationBuilder.ts:21/50/67) never see the union. This satisfies the Developer's and Lorena's hard dealbreakers. Bhishan's fear ("don't couple the daily basics to the risky feature") is honored: Slice 1 ships independently; within Slice 2 the serializer+tests prove byte-stability before any grouped UI exists. |
| 3 | Mixed AND/OR without parens: refuse, or auto-group? | **Refuse + offer explicit `( group these )` button.** No auto-group in this release. | Unanimous across personas (Lal hates overloaded/clever controls, Bhishan wants no surprise auto-wrap, Developer wants no silent rewrite of user logic, Lorena wants refuse-first as the safe default). The serializer must be *structurally incapable* of emitting flat `a or b and c` (a `ParsedExpression` has exactly one combinator; only `GroupedExpression` introduces a second). The legacy fragment-append path that *can* emit flat-mixed today is **deleted** in Slice 2 (see §3.7). |
| 4 | Path-suffix matching in choices merge: this PR or next sprint? | **Next sprint (out of scope).** Row-name join only, this release. Unmatched contact fields fall back to the free-text input (safe, existing behavior). | Row-name match covers ~95% of configs per discovery. All four personas explicitly accept deferral as a documented known-limitation, provided the free-text fallback still appears for unmatched fields (it does — `buildFieldChoices` simply omits unmatched names, and the value cell ternary at FormEditor.tsx:1238-1261 already falls through to `<input>`). |
| 5 | Cross-form scan: synchronous at project-open, or lazy on form-open? | **Synchronous at project-open.** | Sub-100ms for typical projects (3–5 contact forms). Predictable (no surprise slow first-form-open), and the project route already has the directory check (`dirHasFiles` inside `describeProject` at project.ts:45) so the scan slots in naturally. Bhishan and Lorena both said "planner's call, both fine"; Developer explicitly preferred synchronous-for-predictability. Parallelize the per-form parse with `Promise.all` (mirror the `forms.ts` listing pattern). |

---

## 1. Feasibility verdict

**Feasible in two slices.** The parser is already chain-aware **for a single combinator**: `parseRelevant` at `relevantParser.ts:112-134` paren-aware-splits the top level on one combinator via `splitOnCombinator` (lines 144-174). The choices fix is a pure read-only server scan + client merge with zero XLSForm writes.

**Two empirically-verified caveats correct v0.1's overclaims:**

1. **The serializer normalizes whitespace.** `serializeRelevant(parseRelevant("${a}='x' and ${b}>10"))` returns `"${a} = 'x' and ${b} > 10"` — NOT byte-identical to input — with `isRawFallback === false`. Same-combinator chains round-trip byte-stable **only when the input is already in canonical spacing** (spaces around operators, a space after the comma in `selected(...)`). For tighter human spacing the serializer silently reformats without flagging it. This is the dominant hand-authored form in real configs, so the round-trip contract must use a **parse→reserialize→compare self-check** (§3.1) instead of trusting the structured path blindly.

2. **`parseRelevant` short-circuits on mixed combinators BEFORE any split.** Lines 118-121: if both `\band\b` and `\bor\b` appear anywhere (paren-blind regex), it immediately returns a single `RawRule` with `isRawFallback:true`. Verified: `parseRelevant("(${a}='x' and ${b}>10) or ${c}='y'")` returns `isRawFallback:true`, 1 raw rule. Therefore `parseRelevantGrouped` **cannot** "delegate to parseRelevant internals" for grouped input — it must detect the outer parenthesized structure and split on the outer combinator itself, then parse each de-parenthesized subgroup (§4).

The **only** architecturally non-trivial piece is the grouped-expression parser, and the breaking-change risk it carries is fully mitigated by adding a separate entry point rather than widening the existing one (§3). With that mitigation, the blast radius of the parser work is the new FormEditor builder only.

---

## 2. Scope

### In scope

**Slice 1 — Choices fix (read-only):**
- Server-side cross-form contact-choices scan at project open (`server/src/routes/project.ts`).
- `contactFieldChoices: Record<string, string[]>` added to the project payload + client `ProjectInfo`.
- `buildFieldChoices` widening in `FormEditor.tsx` to merge contact choices (row-name join, form-local wins on collision).
- One Playwright test: `sex` value cell is a dropdown (against a committed fixture, runnable on a fresh clone with no env export).

**Slice 2 — Chaining + group affordance:**
- New parser entry point `parseRelevantGrouped(expr): AnyParsed` + grouped serializer — additive, no change to the existing `parseRelevant` signature; both `parseRelevant` and `parseRelevantGrouped` gain a parse→reserialize→compare self-check (§3.1).
- Stacked-clause chaining UI in `UnifiedConditionBuilder` (FormEditor.tsx) with `role="group"` + `aria-label`, comparison-prose preview, and the `( group these )` affordance for parenthesized mixed-combinator expressions (two levels max).
- Reducer + reducer unit tests; grouped round-trip tests; Playwright happy-path + cancel-safety + group e2e.
- Deletion of the legacy fragment-append path in `build()`/`doInsert` (§3.7).
- Microcopy per §10.

### Out of scope (do not build unless asked)
- Path-suffix matching for divergent contact field names (Open Q4 → next sprint).
- Smart list-name guessing; recent-values hint.
- Plain-language prose for `today()` — stays a code-style chip. **`selected()`/`not()` DO get prose** in this release (Bhishan gate, §9) — see §10.
- Drag-to-reorder clauses (Lal P2); backspace-to-delete + roving-tabindex keyboard nav (Lal P1, next sprint). **Click-to-delete ships now, as a focusable labeled `<button>`.**
- Three-plus-level nesting — capped at two levels in the parser, which falls back to raw.
- Auto-group of flat-mixed AND/OR (Open Q3 → refuse-and-offer-button only).
- Present-but-empty extras keys: the existing serializer normalizes a deleted key identically to a previously-absent one (documented in §3.5); we do NOT add present-but-empty preservation.
- Pre-existing 109 lint problems (separate cleanup PR — see §8 gate scoping).

---

## 3. Round-trip + breaking-change safety contract

This is the non-negotiable bar (per CLAUDE.md round-trip invariant). The builder MUST guarantee on every write to `row.extras[column]`:

### 3.1 Byte-stability via a parse→reserialize→compare self-check (MANDATORY)

The serializer canonicalizes spacing, so trusting the structured path blindly **silently reformats tight human input** (verified: `${a}='x' and ${b}>10` → `${a} = 'x' and ${b} > 10`, `isRawFallback:false`). To make byte-stability real:

- Add an internal self-check to **both** `parseRelevant` and `parseRelevantGrouped`: after building the structured result, re-serialize it and compare to the original trimmed input. **If they differ, discard the structured result and return a single `RawRule` carrying the original (trimmed) text, with `isRawFallback:true`.** This guarantees `serialize(parse(x)) === x.trim()` for every non-raw result, and that any expression whose canonical form differs from the author's spelling is preserved verbatim as raw rather than reformatted.
  - **Implementation note:** the self-check compares against `expr.trim()` (parser already trims at line 113). The cancel-safety/round-trip tests therefore assert against the trimmed input. Pre-existing leading/trailing whitespace in `row.extras` is already normalized by `trim()` today; this is unchanged behavior and not introduced here.
- After this change, structured (non-raw) byte-stability is guaranteed by construction. The §6 test buckets are split accordingly: **canonical-spacing strings** assert structured stability; **non-canonical hand-written strings** assert `isRawFallback:true` AND that the raw text serializes byte-identical.

### 3.2 No partial clauses
`+ insert` disabled unless every clause in `clauses[]` AND the non-empty draft has `field`, `op`, and (for ops requiring it) `value`. Per-clause red border + `aria-invalid="true"` on the offender.

### 3.3 No silent connector mixing — serializer-side guarantee, not just a UI gate
A `ParsedExpression` has exactly one combinator; only `GroupedExpression` introduces a second. There must be **no code path** where mixed-combinator rules land in a single `ParsedExpression.rules[]` and serialize as flat `a or b and c`. Flat-mixed (no parens) is refused at the reducer (`commitClause('or')` while `lockedConnector==='and'` is a no-op in flat mode) AND structurally impossible at the serializer. The user mixes AND/OR only by explicitly creating a group. The legacy `build()`/`doInsert` fragment-append path (which *can* emit flat-mixed today) is deleted (§3.7).

### 3.4 Raw fallback is sacred
If `parseRelevant(existing).isRawFallback` is true AND `parseRelevantGrouped` also can't decompose it, chaining is disabled entirely; the builder shows the raw-fallback banner, keeps the existing text **visible and editable** (never blanks it), and offers "edit as text" / "clear to use builder". No silent overwrite.

### 3.5 `× start over` is byte-safe
Clears reducer state only; it **never calls `setColumn`/`setExtra`**, so `row.extras[column]` is untouched until `+ insert` fires. (Verified by Playwright reading saved bytes via the API — see §7 Slice 2 e2e.)

**`setExtra` delete-on-empty caveat (verified at FormEditor.tsx:692-699):** `setExtra` *deletes* `row.extras[key]` when `value === ''` (line 695) rather than writing an empty string. Consequences for this plan:
- The reducer's `insertAll` path routes writes through `setColumn → setExtra`. It must **never** hand `setExtra` an empty string for the active column unless the user intends to clear it — clearing is a delete, which is fine and round-trip-safe, but the builder must not accidentally emit `''` for a non-empty build.
- The cancel-safety e2e (§7) asserts byte-identity at the **XLSForm-serializer level** (parser-level diff via the API), NOT a naive string compare of a possibly-absent key. If a column legitimately held a present-but-empty `''` originally, the editor normalizes it to absent on any write; this is **pre-existing XLSForm-serializer behavior** (a deleted key serializes identically to a previously-absent one for `relevant`/`calculation`/`constraint`/`choice_filter`), so the cancel-safety test must not falsely flag it. `× start over` sidesteps the whole issue by never writing at all.

### 3.6 Choices broadening is read-only
`parseXlsForm` on contact forms is ephemeral; no XLSForm bytes change as a result of contact-form scanning.

### 3.7 Legacy flat-mixed path must be fully removed (Slice 2 regression check)
Today `UnifiedConditionBuilder.build()` (FormEditor.tsx:1128-1143) returns bare ` and `/` or ` fragments and `doInsert` string-concatenates them, which **can already emit flat-mixed `a or b and c` straight to `row.extras[column]`** without going through any parser. Slice 2 replaces this path. The acceptance check (§7) requires confirming the fragment-append-connector path is **deleted**, and that ALL writes to `row.extras[column]` from the builder go through `serializeAnyParsed` (never a hand-concatenated connector). A reducer/e2e assertion verifies that no sequence of UI actions can produce a `row.extras` value where `parseRelevant(value).isRawFallback` became true due to flat-mixed connectors introduced by the builder itself.

### Breaking-change safety (the proposal's central defect — CORRECTED)

The proposal §5 and §9.2 claim the parser change is "widen `parseRelevant` return to `AnyParsed = ParsedExpression | GroupedExpression`". **This is wrong and a hard reject.** Verified against the real call-sites:

- `client/src/ui/RelevantRuleBuilder.tsx:37` — `useState<ParsedExpression>(() => parseRelevant(props.value))`; line 38 same; line 42 `setParsed(parseRelevant(...))`; the `{...parsed, rules}` spreads at lines 48/51 depend on the shape staying byte-identical.
- `client/src/ui/CalculationBuilder.tsx:54` — `condition: parseRelevant('')`; line 173 `condition: parseRelevant(v)`; line 184 `serializeConditionSummary(cond: ParsedExpression)`; line 208 `serializeRelevantStub(cond: ParsedExpression)`.
- `client/src/ui/DecisionsView.tsx:438` — `relevantToHumanLines(cond: ParsedExpression)`.
- `shared/src/xlsform/calculationBuilder.ts:21` — `condition: ParsedExpression` field type; line 50 `serializeRelevant(r.condition)`; line 67 `parseRelevant(cond)` bound to it.

Widening `parseRelevant`'s return type to a union breaks the typed slots above at compile time (GroupedExpression is not assignable to ParsedExpression). **Mandated fix:** keep `parseRelevant(expr): ParsedExpression` and `serializeRelevant(parsed: ParsedExpression): string` byte-identical. Add a NEW export `parseRelevantGrouped(expr): AnyParsed` that only the new FormEditor builder calls, plus `serializeAnyParsed(parsed: AnyParsed): string`. `shared/src/index.ts` re-exports `./xlsform/relevantParser.js`, so new exports are zero-config.

**HARD RULE (not optional):** do NOT add any new *required* field to `ParsedExpression`. Discriminate `AnyParsed` via `'subgroups' in parsed`. Adding a required `kind:'flat'` to `ParsedExpression` would break the `{...parsed, rules}` spreads at RelevantRuleBuilder.tsx:48/51 — so it is forbidden. `typecheck` green on all 5 consumers is the proof gate.

Result: the union is genuinely additive; the 5 consumers and all 85 shared tests stay untouched.

---

## 4. File-by-file task list (REAL anchors)

> All line numbers below are verified against the current tree.

### Slice 1 — Choices fix

**`server/src/routes/project.ts`** (modify) — verified path `d:/cht-ui-builder/server/src/routes/project.ts`.
- Add `contactFieldChoices: Record<string, string[]>` to the `ProjectInfo` interface (currently lines 11-19, **7 fields: `path`, `name` (2 strings) + 5 booleans `hasAppSettings`/`hasAppForms`/`hasContactForms`/`hasTasks`/`hasContactSummary`**; no choices field).
- In `describeProject` (lines 39-49), after the existing flag computation, scan `forms/contact/*.xlsx`: `readdir`, filter `.xlsx`, parse each with `parseXlsForm` (import it as `forms.ts:172` does), collect rows whose `type` matches `/^(select_one|select_multiple)\s+(\S+)/i`, resolve choice values from the form's own `choices` sheet keyed by `list_name`, build the map keyed by row `name`. **Parallelize with `Promise.all`** (mirror the `forms.ts` listing pattern). On collision across forms, last-write-wins (documented limitation; no warning this release).
- Attach to the returned payload. Both `/api/project` (line 60) and `/api/project/open` (line 85) call `describeProject`, so both paths get it for free.
- **~45 LOC.** Read-only; zero XLSForm writes.

**`client/src/state/store.ts`** (modify) — verified path `d:/cht-ui-builder/client/src/state/store.ts`, `ProjectInfo` at lines 23-31.
- Add `contactFieldChoices: Record<string, string[]>` to the `ProjectInfo` interface. **~1 LOC.**
- No `client/src/api.ts` change needed: `client/src/api.ts:6` imports `ProjectInfo` from store and reuses it for both `getProject` (lines 39-40) and `openProject` (lines 42-46). Extending the one interface flows through both. **(Path note for the developer: the API module is `client/src/api.ts`, NOT `client/src/state/api.ts`; `store.ts` edited in the same commit lives in `client/src/state/`.)**

**`client/src/ui/FormEditor.tsx`** (modify) — verified.
- `buildFieldChoices` is at **lines 1052-1068**. It currently resolves choices only from the open form's own select rows (regex filter at line 1062, `select_one`/`select_multiple` only). Change its signature to also accept the project's `contactFieldChoices` and merge: start from the contact map, then overlay form-local results so **form-local wins on name collision** (project context must not override the form's own definition). Handle `contactFieldChoices === undefined` gracefully (older server responses).
- `fieldChoices` is built at **line 297** via `useMemo(() => buildFieldChoices(form.survey, form.choices), ...)`. Add `contactFieldChoices` (read from `useApp().project`) as a third arg + dep.
- Keep the free-text fallback intact. The actual fallback is the `else` branch of the value-cell ternary at **lines 1238-1261** (the `<input>` opens at line 1254). No change needed there; it falls through automatically for unmatched fields.
- **~25 LOC.**

### Slice 2 — Chaining + group affordance

**`shared/src/xlsform/relevantParser.ts`** (modify) — verified.
- Do **NOT** change the *signatures* of `parseRelevant` (line 112, returns `ParsedExpression`) or `serializeRelevant` (line 137, param `ParsedExpression`). They stay byte-identical in shape and remain the 5 consumers' contract.
- **Add the §3.1 self-check inside `parseRelevant`** (after line 132, before the return): re-serialize the candidate structured result and, if `serializeRelevant(candidate) !== trimmed`, return `{ combinator: candidate.combinator, rules: [{ kind: 'raw', text: trimmed }], isRawFallback: true }` instead. This is internal behavior, not a signature change. (It will reclassify tight-spacing inputs as raw; the 85 existing shared tests use canonical spacing and stay green — verify, see §8.)
- Add interfaces:
  ```ts
  export interface GroupedExpression {
    kind: 'grouped';
    outerCombinator: Combinator;
    subgroups: ParsedExpression[];  // each a same-combinator chain; non-recursive (two levels max)
    isRawFallback: boolean;
  }
  export type AnyParsed = ParsedExpression | GroupedExpression;
  ```
  Discriminate via `'subgroups' in parsed` (no `kind` added to `ParsedExpression` — §3 HARD RULE).
- Add `parseRelevantGrouped(expr): AnyParsed` as a **fresh top-level function** (do NOT delegate to `parseRelevant` for grouped input — it short-circuits to raw on mixed combinators at lines 118-121):
  1. Trim. Detect whether the expression has an outer parenthesized mixed-combinator structure: it contains a top-level combinator AND at least one top-level parenthesized group, and both `and` and `or` appear. If no outer grouping is detected → fall through to flat `parseRelevant(expr)` (which now self-checks per §3.1).
  2. Determine the outer combinator (the top-level one **outside** all parens). Call `splitOnCombinator(expr, outerCombinator)` directly — it is paren-aware and correctly yields e.g. `["(${a}='x' and ${b}>10)", "${c}='y'"]` (verified).
  3. For each part: strip exactly one balanced outer paren pair if the part is wholly wrapped, then `parseRelevant` the inner chain. A subgroup that is itself a parenthesized different-combinator group, or whose inner parse is `isRawFallback`, forces the **whole expression** to `isRawFallback:true` (two-levels-max + safety).
  4. **Apply the §3.1 self-check to the grouped result too:** if `serializeAnyParsed(grouped) !== trimmed`, return a single-`RawRule` `ParsedExpression` with `isRawFallback:true` carrying the original trimmed text. This is what makes inner-padded parens (`( A and B ) or C`), tight operator spacing, and leading/trailing-paren variants route to raw instead of being reformatted.
- Add `serializeAnyParsed(parsed: AnyParsed): string`: if `'subgroups' in parsed`, wrap each subgroup as `(serializeRelevant(subgroup))` and join with ` ${outerCombinator} ` (canonical form `(A op B) or C`); otherwise delegate to `serializeRelevant`. MUST be structurally incapable of emitting flat mixed combinators (§3.3).
- **~95 LOC parser (incl. self-check) + ~30 LOC serializer.**

**`shared/src/xlsform/relevantParser.chain.roundtrip.test.ts`** (new). See §6 case list. Two buckets: canonical strings assert `serializeAnyParsed(parseRelevantGrouped(x)) === x` byte-for-byte; non-canonical strings assert `isRawFallback:true` and raw byte-identity. **~170 LOC.**

**`client/src/ui/FormEditor.tsx`** (modify) — verified anchors.
- `UnifiedConditionBuilder` component definition starts at **line 1103** (JSDoc above at 1094; JSX render 1174-1307; function ends 1308). `CondOp` type at 1070-1082; `COND_OPS_NEED_FIELD`/`COND_OPS_NEED_VALUE` at 1084-1085; `COLUMN_OPTIONS` at 1087-1092; transient state `{column, field, op, value, lastInsert}` at 1109-1116; `build()` at 1128-1143; `doInsert` string-concat at ~1150; the value-cell ternary (dropdown vs free-text) at 1238-1261; the buttons at 1262-1304; `getColumn`/`setColumn` props at 1106-1107.
- Refactor the transient state to the reducer `BuilderState` (§5). **Separate the connector from the op** (Lal dealbreaker): the per-clause op dropdown (currently 1207-1237) must NOT contain `and`/`or` in ANY mode — strip the `<optgroup label="logic">` `and`/`or` options entirely (including the n===1 strip); the connector becomes a between-clause pill rendered between clause rows. `not` stays in the op list. **Delete the `build()`/`doInsert` fragment-append connector path (§3.7); all writes go through `serializeAnyParsed`.**
- **One-clause fidelity contract (Lal blocker), CORRECTED:** when `clauses.length===1 && draft is empty && groups===null && !rawFallback`, render the SAME single horizontal strip as today (same column/field/value dropdowns, same free-text fallback at the 1238-1261 ternary, same `+ insert` position) **with exactly one intentional delta: the op dropdown no longer contains `and`/`or`** (they move to the between-clause connector pill in all modes, including the implicit first clause). This is the only difference from today's strip; it is required to close the §3.3/§3.7 hole (the legacy op-dropdown `and`/`or` fed the flat-mixed append path). The before/after screenshot pair **must call out this one delta** rather than claiming pixel-identity. No chip group, no "add another rule" weight, no preview header at n===1. The stacked-clause/chip UI only appears once a second clause exists.
- Multi-clause container: `role="group"` + `aria-label="Conditions for showing this row"` (static markup, ships now). Each clause's delete is a real focusable `<button>` with `aria-label="remove rule"` (NOT a bare `×` span). Backspace-delete + roving tabindex deferred (Lal P1).
- `( group these )` is a **standing, low-emphasis visible button** whenever `clauses.length>=2` (discoverability — Lal dealbreaker: not gated behind tripping the flat-mixed warning). The flat-mixed warning copy remains as the safety net, not the only entry point.
- **Prose preview header `This row shows when:`** rendered live: comparison rules AND `selected()`/`not()` render as prose (e.g. "symptoms includes Fever", "X is not selected") so the whole line reads as a sentence (Bhishan gate, §9); `today()`/date-offset/age stay code-style chips.
- Rehydrate from `parseRelevantGrouped` on row open. Raw-fallback → banner + keep text editable + chaining disabled.
- Caller: `UnifiedConditionBuilder` is invoked from `SurveyRowCard` at **line 774** with `fieldOptions`/`fieldChoices` props (line 776). Keep that prop contract; the `getColumn`/`setColumn` callbacks (props 1106-1107) stay.
- **~250 LOC UI + ~80 LOC reducer wiring + ~120 LOC group affordance.**

**`client/src/ui/RelevantRuleBuilder.tsx`** (modify — connector copy alignment, Lal must-have). **Line 129** holds `ALL rules must match (AND)`; **line 138** holds `ANY rule may match (OR)` (line 136 is the OR radio's `onChange`, NOT the copy — corrected from v0.1). Align both to `and also` / `or instead` to match the new strip (~2 strings). If the implementer chooses not to, the PR description MUST explicitly note the divergence and schedule it — silent divergence is a reject. **~2 LOC.**

**Client reducer + unit tests** (new file, e.g. `client/src/ui/conditionReducer.ts` + `conditionReducer.test.ts`). Reducer actions: `commitClause`, `popClause`, `startOver`, `insertAll`, `enterGroupMode`, `exitGroupMode`, `setDraft`. **~80 LOC reducer + ~80 LOC tests.**

**`client/tests/setup.ts`** (modify) + new fixture (Lorena blocker). `PROJECT_PATH` currently hardcodes `W:\medic\config-gandaki\cht-config` (line 8) which doesn't exist on a fresh clone. Commit a minimal fixture project at `client/tests/fixtures/mini-config` (tiny `forms/app/` + `forms/contact/` with at least a `select_one sex` field whose choices are `male/female/other` in a contact form), and **default `PLAYWRIGHT_PROJECT_PATH` to the committed fixture path resolved relative to the repo** so NO manual env export is needed on a fresh clone. **Slice 1 needs this** (its e2e asserts the populated dropdown).

**`client/tests/condition-builder.spec.ts`** (new). Slice 1: dropdown-populated test. Slice 2: happy-path, cancel-safety (XLSForm-serializer-level byte identity), group affordance. **~120 LOC.**

---

## 5. Data shapes

### Reducer state (transient, NOT persisted — `SurveyRow.extras` stays `Record<string,string>`)

```ts
type Clause = {
  field: string;
  op: '=' | '!=' | '>' | '<' | '>=' | '<=' | 'selected' | 'not' | 'ref' | 'today';
  value: string;  // empty for ops in {'ref','today','not'}
};
type Connector = 'and' | 'or';
type Subgroup = { clauses: Clause[]; connector: Connector };  // intra-subgroup, same combinator

interface BuilderState {
  column: 'relevant' | 'calculation' | 'constraint' | 'choice_filter' | '';
  clauses: Clause[];               // flat single-combinator chain (common case)
  connectors: Connector[];         // length === max(0, clauses.length - 1)
  draft: Clause;
  lockedConnector: Connector | null;
  groups: Subgroup[] | null;       // null = flat mode; non-null = grouped (two levels max)
  outerConnector: Connector | null;
  activeGroupIndex: number | null;
  rawFallback: string | null;      // non-null = chaining disabled, text preserved verbatim
}
```

### Parser extension shape (`relevantParser.ts`) — ADDITIVE

```ts
// SIGNATURES UNCHANGED — 5 consumers depend on these shapes; internal self-check added (§3.1):
export interface ParsedExpression { combinator: Combinator; rules: Rule[]; isRawFallback: boolean }
export function parseRelevant(expr: string): ParsedExpression { /* + self-check (§3.1) */ }
export function serializeRelevant(parsed: ParsedExpression): string { /* unchanged */ }

// NEW — additive:
export interface GroupedExpression {
  kind: 'grouped';
  outerCombinator: Combinator;
  subgroups: ParsedExpression[];   // each is a same-combinator chain; non-recursive (two levels max)
  isRawFallback: boolean;
}
export type AnyParsed = ParsedExpression | GroupedExpression;   // discriminate via 'subgroups' in parsed
export function parseRelevantGrouped(expr: string): AnyParsed { /* fresh fn: outer-split → de-paren → parseRelevant inner → self-check */ }
export function serializeAnyParsed(parsed: AnyParsed): string { /* grouped-aware; canonical (A op B) or C */ }
```

`subgroups: ParsedExpression[]` is structurally non-recursive (a subgroup cannot itself be grouped) — this is the two-levels-max enforcement at the type level.

---

## 6. Test plan (round-trip cases — byte-for-byte, split into structured vs raw buckets)

`shared/src/xlsform/relevantParser.chain.roundtrip.test.ts`:

**Bucket A — canonical strings, assert STRUCTURED byte-stability** (`serializeAnyParsed(parseRelevantGrouped(x)) === x`, `isRawFallback:false`). All written in the serializer's canonical spacing (spaces around operators, space after comma in `selected()`), matching the existing `relevantParser.dates.test.ts` convention:
- Three-clause AND: `${sex} = 'female' and ${age} > 18 and selected(${conds}, 'x')`.
- Three-clause OR: same with ` or `.
- Mixed `selected()` + comparison + `not()` under a single `and`: `${sex} = 'female' and selected(${conds}, 'x') and not(selected(${conds}, 'none'))`.
- Grouped: `(${a} = 'x' and ${b} > 10) or ${c} = 'y'` → `GroupedExpression`, byte-stable.
- Grouped reverse: `${a} = 'x' or (${b} > 10 and ${c} = 'y')`.
- Three-subgroup: `(A and B) or (C and D) or E` (canonically spelled) → `outerCombinator:'or'`, three subgroups, byte-stable.

**Bucket B — non-canonical / out-of-grammar strings, assert `isRawFallback:true` AND raw byte-identity** (`serializeAnyParsed(parseRelevantGrouped(x)) === x.trim()` AND result is a single `RawRule`):
- **Tight-spacing flat chain:** `${a}='x' and ${b}>10` → raw, byte-identical (this is the §3.1 self-check firing; documents that tight human spacing is preserved verbatim, NOT reformatted).
- **Inner-padded parens:** `( ${a} = 'x' and ${b} > 10 ) or ${c}='y'` → raw, byte-identical (serializer cannot reproduce inner padding; self-check routes to raw).
- **Flat-mixed, no parens:** `${a}='x' or ${b}>10 and ${c}='y'` → raw, byte-identical (mixed-combinator short-circuit, §1 caveat 2).
- **Two-levels-max refusal:** `((A and B) or C) and D` → single `RawRule`, byte-identical.
- **Paren group containing flat-mixed:** `(A and B or C) or D` → raw, byte-identical.
- Leading/trailing stray parens that don't form a clean group → raw, byte-identical.

**Bucket C — structural edge cases:**
- Per-clause raw inside a single-combinator AND chain (canonical spacing): that clause is `RawRule`, siblings stay structured, byte-stable via the chain serializer.
- Edges: empty string (`''` → empty rules), single canonical clause, only-whitespace (→ empty), parens-only `()`.

**Field-coverage discovery gate (Developer must-have, NEW):** before Slice 2 ships, grep the available cht-conf config(s) for parenthesized boolean groups in `relevant`/`constraint`/`choice_filter` columns. If any real hand-written grouped expression exists, add it verbatim as a round-trip fixture here (it will land in Bucket A if canonical, Bucket B if not). If none exist, **record that explicitly in the PR as a known field-coverage gap** (the grouped shape is test-covered only, not field-covered) rather than leaving it implicit.

Client reducer unit tests (`conditionReducer.test.ts`):
- `commitClause` pushes draft, sets `lockedConnector`.
- `popClause` pops last clause + last connector together.
- **`startOver` does NOT touch `column` and never calls `setColumn`/`setExtra`** (assert no write side-effect — §3.5).
- `insertAll` with empty draft serializes `clauses` only and **never emits a dangling connector**; non-empty draft includes it; assert the produced string round-trips through `parseRelevant` with `isRawFallback:false` (i.e. the builder never emits a flat-mixed string — §3.7).
- **`commitClause('or')` when `lockedConnector==='and'` is a no-op in flat mode** (proves flat `a or b and c` can't be emitted — §3.3) and surfaces the convert-to-group offer.
- `enterGroupMode` migrates `clauses` into `groups[0]`, sets `outerConnector`.
- **Raw-fallback expression disables chaining entirely** (§3.4).

Playwright e2e (`condition-builder.spec.ts`, against the committed fixture, no env export needed):
- **Slice 1:** open form, `sex` value cell is a `<select>` with `male/female/other` (not `<input>`).
- **Slice 2 happy path:** build `${sex} = 'female' and ${age} > 18`, save, reload, reopen row, see two chips.
- **Slice 2 cancel-safety:** load row with existing single-clause `relevant`, stage two new clauses, press `× start over`, save; assert `row.extras['relevant']` read via API is byte-identical to load **at the XLSForm-serializer level** (parser-level diff, not a naive string compare of a possibly-deleted key) — Lorena's #1 regression, accounting for the `setExtra` delete-on-empty semantics (§3.5).
- **Slice 2 group:** build `${a} = 'x' and ${b} > 10`, click `( group these )`, add `or ${c} = 'y'`, insert, reload, see grouped chip rendering.
- **Slice 2 no-flat-mixed:** assert there is no UI sequence that writes a `row.extras` value parsing to `isRawFallback:true` via builder-introduced flat-mixed connectors (§3.7).

Smoke test:
- **Fresh-clone smoke (primary):** `node scripts/smoke-parser.mjs client/tests/fixtures/mini-config/forms/app/<form>.xlsx` → `Round-trip stable: YES`. This runs green on a fresh clone with no sibling config.
- **Real-config smoke (optional, original-machine only):** `node scripts/smoke-parser.mjs <real-config>/forms/app/pregnancy.xlsx` → `Round-trip stable: YES`.
- Slice 2: additionally exercise `parseRelevantGrouped` against `(A and B) or C` via the round-trip test (real configs may not contain a grouped relevant; see field-coverage discovery gate above).

---

## 7. Phasing / slice plan with acceptance criteria + validate commands

### Slice 1 — Choices fix (this week, ~80 LOC). MUST contain ZERO `GroupedExpression`/`parseRelevant` changes.

Commits: (1) server scan + payload, (2) store + FormEditor merge, (3) fixture + e2e.

Acceptance:
- `sex` (contact-injected) value cell renders a dropdown with the contact form's choices; unmatched fields still show free-text input.
- No XLSForm bytes change from scanning (read-only).
- `parseRelevant` signature/behavior, the 85 shared tests, and the 5 consumers untouched.

Validate (fresh-clone runnable, no env export):
```sh
pnpm --filter @cht-ui/shared build
pnpm typecheck
pnpm --filter @cht-ui/shared test            # 85 shared green (unchanged)
node scripts/smoke-parser.mjs client/tests/fixtures/mini-config/forms/app/<form>.xlsx   # Round-trip stable: YES
pnpm --filter @cht-ui/client test:e2e        # dropdown-populated test (defaults to committed fixture)
```

### Slice 2 — Chaining + group affordance (~570 LOC).

Commits in order: (A) parser self-check + `parseRelevantGrouped`/`serializeAnyParsed` + grouped round-trip tests (proves byte-stability BEFORE any grouped UI exists — Developer/Lorena dealbreaker), (B) reducer + reducer tests + flat-chaining UI + legacy `build()`/`doInsert` deletion + RelevantRuleBuilder copy alignment, (C) group affordance UI + group e2e.

Acceptance:
- §3 contract holds: byte-stable structured path via the self-check; non-canonical/out-of-grammar input routes to raw byte-identical; no flat-mixed emission (reducer + serializer + legacy path deleted); raw fallback sacred; start-over byte-safe (no `setExtra` call).
- One-clause path visually identical to today's strip **except the op dropdown has no `and`/`or`** (before/after screenshots call out that single delta).
- Multi-clause container `role="group"` + `aria-label`; per-clause delete a focusable labeled `<button>`.
- `( group these )` visible whenever `clauses.length>=2` (not error-gated).
- Connector separated from op in all modes; op dropdown has no `and`/`or`.
- Comparison AND `selected()`/`not()` read as prose; `today()`/age/date-offset stay code chips.
- Legacy fragment-append path deleted; all builder writes go through `serializeAnyParsed`.
- New round-trip (Buckets A/B/C) + reducer + e2e tests green; field-coverage discovery recorded.

Validate (fresh-clone runnable, no env export):
```sh
pnpm --filter @cht-ui/shared build
pnpm --filter @cht-ui/shared test            # 85 existing + ~14 new grouped/chain cases (~99 shared)
pnpm typecheck                               # clean — proves additive (no consumer breaks)
pnpm lint                                    # scoped: no NEW warnings/errors on changed files (see §8)
node scripts/smoke-parser.mjs client/tests/fixtures/mini-config/forms/app/<form>.xlsx   # Round-trip stable: YES
pnpm --filter @cht-ui/client test:e2e        # happy + cancel-safety + group + no-flat-mixed
```

---

## 8. Gates (CORRECTED from proposal + v0.1)

- **Shared tests: 85 existing green (NOT 47).** Verified: **85** `test()` cases across 9 files (renameList 18, fhir/key 17, hierarchyOrder 10, fhir/roundtrip 10, appliesIfParser 8, relevantParser.dates 6, fhir/reconcile 6, inlineChoices 5, fhir/starterPack 5), plus **19 server tests = 104 total**. State the gate as "85 shared / 104 total". Slice 2 adds ~14 new shared cases (~99 shared then).
- **Self-check regression guard:** after adding the §3.1 self-check to `parseRelevant`, re-run the 85 shared tests — they use canonical spacing and MUST stay green. If any existing test asserts a structured parse of a tight-spaced string, that test now expects raw; reconcile it explicitly (do not silently change an existing assertion).
- New `relevantParser.chain.roundtrip.test.ts` green (Buckets A/B/C); reducer unit tests green.
- `pnpm typecheck` clean (this is the proof the parser change is truly additive — if it's red on RelevantRuleBuilder/CalculationBuilder/DecisionsView/calculationBuilder, the signature or `ParsedExpression` shape was wrongly changed).
- **Lint gate scoped (Lorena dealbreaker):** repo has 109 pre-existing problems (101 errors, 8 warnings) — pre-existing, separate cleanup PR. Do NOT list `pnpm lint --max-warnings=0` as wholesale-green. The honest gate is: **zero NEW lint warnings/errors introduced by this diff's changed files.**
- Smoke parser `Round-trip stable: YES` on the **committed fixture** at every commit (fresh-clone runnable); real-config smoke is an additional optional check on the original machine.
- Playwright suite green against the committed fixture, runnable with no manual env export.
- **Legacy-path-removed check (§3.7):** confirm the `build()`/`doInsert` fragment-append connector path is deleted and no builder write bypasses `serializeAnyParsed`.

---

## 9. Persona acceptance gates

**Bhishan (PO/PM):**
- Cold-start "show when sex is female AND age > 18" in <30s, no help text. Microcopy + prose carry the load.
- `sex` value cell is a dropdown (Slice 1), never free-text, when pulled from contact.
- Prose preview reads as a full sentence for his two most common clause types — comparisons AND `selected()`/`not()` (the latter previously rendered as a code chip; now prose, e.g. "symptoms includes Fever"). `today()`/age/date-offset remain code chips (accepted limitation, signed off here). Live, before commit.
- Raw-fallback never silently eats a hand-written expression; tight-spaced hand input is preserved verbatim as raw (§3.1) rather than reformatted; `× start over` only clears in-progress build and never writes.
- Single-clause case looks/behaves like today (one intentional delta: no `and`/`or` in the op dropdown).
- Daily basics (Slice 1 + flat chaining) are not coupled to the risky group feature — Slice 1 ships standalone; within Slice 2 the serializer+tests land before grouped UI.

**Lal (Designer):**
- One-clause fidelity contract written + before/after screenshots in PR, with the single intentional delta (no `and`/`or` in op dropdown) explicitly called out.
- a11y contradiction resolved: `role="group"` + `aria-label` + tabbable labeled `<button>` deletes ship NOW; only backspace-delete + roving-tabindex deferred (worded as "backspace-to-delete deferred", not "chip group deferred").
- `( group these )` discoverable (standing visible button at clauses≥2), not error-gated.
- Connector copy `and also`/`or instead`; RelevantRuleBuilder.tsx:129 (AND) and :138 (OR) aligned (or divergence explicitly scheduled).
- Connector separated from op in ALL modes; no `and`/`or` in the clause op dropdown.
- Raw fallback keeps text visible/editable; start-over byte-safe.

**Lorena (QA):**
- Test count corrected to 85 shared / 104 total everywhere.
- Parser change additive (separate `parseRelevantGrouped`/`serializeAnyParsed`, no `kind` added to `ParsedExpression`); typecheck green on all 5 consumers.
- Committed deterministic fixture; e2e AND smoke runnable on a fresh clone with no manual env export.
- All §6 cases split into Bucket A (structured byte-stable, canonical spacing) and Bucket B (non-canonical → raw byte-identical via the self-check), including flat-mixed-stays-raw, inner-padded-parens-stays-raw, and per-clause-raw-keeps-siblings.
- Reducer tests assert start-over byte-safety (no `setExtra` call), no-dangling-connector, `commitClause('or')`-while-locked no-op, raw disables chaining, and that `insertAll` output never parses to `isRawFallback:true`.
- Cancel-safety e2e compares saved bytes at the XLSForm-serializer level (parser-level diff), accounting for `setExtra` delete-on-empty.
- Lint gate scoped to "no new warnings."

**Developer (parser owner):**
- Existing `parseRelevant`/`serializeRelevant` signatures untouched; new entry point only; `parseRelevantGrouped` is a fresh function that does NOT delegate to `parseRelevant` for grouped input (which short-circuits to raw on mixed combinators at lines 118-121).
- Byte-stability proven by the parse→reserialize→compare self-check (§3.1): the structured path is guaranteed byte-stable; any spelling the serializer can't reproduce (tight spacing, inner-padded parens, leading/trailing parens) routes to raw, asserted in Bucket B.
- Serializer structurally cannot emit flat mixed combinators; legacy fragment-append path deleted (§3.7).
- Raw-fallback tested (3-level + flat-mixed-in-paren + flat-mixed-no-paren → single RawRule, byte-identical).
- Two-levels-max enforced in the parser (via `subgroups: ParsedExpression[]` non-recursive type + step-3 refusal), not just the reducer.
- Field-coverage discovery gate run; result (fixtures added or gap recorded) in the PR.

---

## 10. Microcopy (verbatim — keep exactly)

| Surface | Copy |
|---|---|
| Chain-extender button | `+ add another rule` |
| Connector options | `and also` / `or instead` (serialize as `and`/`or`) |
| Preview header | `This row shows when:` |
| Comparison prose | `<field> is <value>` / `<field> is more than <value>` etc. |
| selected() prose | `<field> includes <value>` |
| not(selected()) prose | `<field> does not include <value>` |
| Cancel button | `× start over` |
| Per-clause delete | `× remove rule` (a focusable `<button>` with `aria-label="remove rule"`) |
| Group affordance button | `( group these )` |
| Exit grouped mode | `flatten` |
| Raw-fallback banner | `This rule was hand-written. Edit as text, or clear it to use the builder.` |
| Flat-mixed AND/OR warning | `Mixing "and also" with "or instead" needs grouping. Press ( group these ) to combine rules.` |

---

## 11. Next concrete deliverable

**Start Slice 1.** Deliver, in this order:
1. `server/src/routes/project.ts` — contact-form scan + `contactFieldChoices` on the payload (parallelized, read-only); extend `ProjectInfo` (lines 11-19, 7 fields → 8).
2. `client/src/state/store.ts` — `contactFieldChoices` on `ProjectInfo` (lines 23-31; flows through `client/src/api.ts` automatically).
3. `client/src/ui/FormEditor.tsx` — widen `buildFieldChoices` (lines 1052-1068) + the `useMemo` at line 297 (form-local wins on collision; undefined-safe).
4. Commit a minimal `client/tests/fixtures/mini-config` (forms/app + forms/contact with `select_one sex` → male/female/other) and default `PLAYWRIGHT_PROJECT_PATH` to the committed fixture path (no env export needed) in `client/tests/setup.ts:8`.
5. One Playwright test: `sex` value cell is a populated dropdown.

Validate Slice 1 with the §7 Slice 1 commands (fresh-clone runnable). Only after Slice 1 is green (and contains zero parser changes) begin Slice 2, sequencing: parser self-check + `parseRelevantGrouped`/`serializeAnyParsed` + round-trip tests → flat chaining (with legacy path deleted) → group affordance.

---

## Addendum v0.3 (2026-06-09) - type-aware field/op filtering + natural-language operator labels

> **Status:** PLANNER-LOCKED, developer-ready. Appends to plan v0.2. Both features are **UI-only**, land in **Slice 2**, and touch **zero** XLSForm bytes and **zero** parser/serializer signatures. They are pure render/affordance changes plus one new client-side-consumed pure utility.
>
> **Anchor correction (read first):** the v0.2 line numbers in §4 are stale — the Slice 2 builder has since been implemented, so the current tree differs. The REAL anchors used by this addendum, verified against the working tree:
> - `buildFieldChoices` — `client/src/ui/FormEditor.tsx:1077-1100` (regex `select_one|select_multiple` at **line 1094**).
> - `earlierFields` computation (drops type via `.map(r => r.name)`) — `client/src/ui/FormEditor.tsx:520-523` and `547-550`.
> - `SurveyRowCard` props incl. `fieldOptions: string[]` / `fieldChoices` — `FormEditor.tsx:679`, with the `<UnifiedConditionBuilder>` JSX invocation at **`FormEditor.tsx:789-794`** (`fieldOptions` at `790`, `fieldChoices` at `791`). (NOTE: the `1034-1050` range is the `RelevantRuleBuilder`/`CalculationBuilder` invocation inside `ExpressionField`, NOT `UnifiedConditionBuilder` — the v0.2-era attribution was wrong and is corrected here.)
> - `UnifiedConditionBuilder` props `fieldOptions`/`fieldChoices` — `FormEditor.tsx:1178-1179`.
> - Field `<select>` render — `FormEditor.tsx:1331-1335`.
> - Op `<select>` render (raw symbol/XPath labels) — `FormEditor.tsx:1337-1363` (option labels at `1344-1362`).
> - Value-cell ternary (dropdown vs `<input>`) — `FormEditor.tsx:1364-1387`.
> - `CONNECTOR_LABELS` — `FormEditor.tsx:1125`; `COMPARISON_PROSE` — `1132-1139`; `clauseToProse` — `1141-1150`.
> - `ClauseOp` union (the closed op set, **exactly 11 values**) — `shared/src/conditionBuilder/conditionReducer.ts:44-55`.
> - `QUESTION_TYPES` / `STRUCTURAL_TYPES` / `isStructural` — `shared/src/xlsform/types.ts:104-134`. `SurveyRow.type` is a bare `string` (`types.ts:21`).
> - Contact scan stores choice values ONLY (`Record<string,string[]>`, no type) — `server/src/routes/project.ts` scan (no change in this addendum).

### 1. Design decisions (locked — do not re-decide)

| # | Question | Decision | Rationale |
|---|----------|----------|-----------|
| A1 | Soft-filter vs hard-hide? | **SOFT only.** Type-incompatible fields stay in the DOM and remain selectable; they are visually de-emphasized + carry a "less common for this check" marker. A persistent **"Show all fields"** toggle is always rendered while filtering is active. The currently-selected field and any rehydrated saved field/op pairing always render intact. | Bhishan + Lal + Lorena + Developer unanimous dealbreaker: hard-hide is the cold-start abandonment trigger and strands mis-classified, `calculate`, contact-injected, and `${other_field}` fields. A wrong heuristic must never remove a real field with no recovery. |
| A2 | Contact-injected & untypeable fields | **Bucket = `unknown` → always-pass.** No server scan change. Contact fields (no type metadata), `calculate` (no inherent type), `${other_field}` reference values, and any unrecognized/compound type fall into `unknown`, which is compatible with **every** operator and is never de-emphasized. | Server scan captures choice values only; guessing a contact field's type is a Developer/Lorena reject. `unknown` is a first-class always-pass bucket — graceful degradation by construction. Contact `select` fields are still detectable as `choice` via the presence of `fieldChoices[name]` (the existing Slice 1 signal); see §5 for the explicit choice-upgrade rule — `selected`/`selected-not` then narrow to them. |
| A3 | Single source of truth for op↔type | **One exported `const OP_FIELD_KINDS: Record<ClauseOp, FieldKind[]>`** keyed by the real `ClauseOp` union (`conditionReducer.ts:44-55`). Field-first narrowing is **DERIVED** from this same const (`opsTypicalForKind(kind)` filters `OP_FIELD_KINDS`), never a parallel table. `Record<ClauseOp,...>` makes adding a future op a compile error until mapped. | Developer dealbreaker: two copies drift. TS `Record` over the closed union is the proof gate. |
| A4 | Field-kind classifier | **One exported pure fn `inferFieldKind(type: string): FieldKind`** in `shared/src/xlsform/types.ts` (beside `isStructural`). It reuses the **existing** `select_one\|select_multiple` suffix regex (factored out of `buildFieldChoices:1094`) — no third copy. Co-domain is a closed enum `'text'\|'numeric'\|'date'\|'choice'\|'geo'\|'unknown'`. | Developer dealbreaker: no third regex copy; classifier domain pinned to the bare `SurveyRow.type` string, co-domain pinned to a closed enum, single-sourced. |
| A5 | Label changes = display-only? | **Yes — render layer only.** Op-dropdown option **labels** become natural language; the option **`value`** stays the canonical `ClauseOp` (one of exactly 11 values). Connector/comparison microcopy reuses the EXISTING `COMPARISON_PROSE`/`CONNECTOR_LABELS` consts (no fourth microcopy source). `clauseToRule`/`serializeAnyParsed` never see a label string. | Grounding bundle 4 proves zero serialization path touches labels; `Clause.op` is the 11-value union. Pure rename, no round-trip risk. |
| A6 | Marker = color alone? | **No. Labeled affordance, delivered via `<optgroup label="…">` section headings.** Because native `<option>` cannot reliably host rich a11y markup (`title`/`aria-label` on a bare `<option>` are not surfaced by screen readers, and a `<span>` badge cannot nest inside an `<option>`), the accessible affordance IS the `<optgroup>` section-heading text ("Typical for this check" vs "Other fields"). It is text-only, greyscale-legible, and never color-only. Two states only ("typical" vs "show-all-anyway"). Per-row `title`/`aria-label` badges are NOT used unless a custom listbox is adopted (deferred, P2). Severity-graded markers deferred (P2). | Lal dealbreaker: color alone fails a11y; the `<optgroup>` text heading satisfies the greyscale/non-color requirement and is actually deliverable with the chosen native-`<select>` approach. |
| A7 | Filtering mutates draft state? | **Never.** Filtering changes the option **list ordering / grouping / styling only**. It must not mutate `state.draft.field` or `state.draft.op`, must not auto-clear a chosen field when the op changes class (or vice versa), and rehydration from `parseRelevantGrouped` bypasses the filter entirely. | Developer dealbreaker: silent rewrite of user intent / desync from a rehydrated saved expression. |

### 2. Operator → compatible field-kind map (FULL taxonomy)

`FieldKind` co-domain: `text | numeric | date | choice | geo | unknown`. `unknown` is compatible with EVERY op (always-pass) and is therefore **omitted from the per-op lists below** — it is injected at filter time as universally compatible. **Crucially, the de-emphasis predicate treats ANY `FieldKind` value that is absent from an op's list exactly like `unknown` (always-pass).** This means a future 7th `FieldKind` (e.g. `media`) not listed in any `OP_FIELD_KINDS` entry will be always-pass, never silently de-emphasized — the never-de-emphasize contract is keyed to "absent from the op's list ⇒ pass", not to the literal string `'unknown'`. The map is the single source of truth (`OP_FIELD_KINDS: Record<ClauseOp, FieldKind[]>`).

**`OP_FIELD_KINDS` (op → kinds the op is "typical" for):**

| `ClauseOp` value | Dropdown label (§4) | Typical field kinds |
|---|---|---|
| `=` | `equals` | text, numeric, date, choice |
| `!=` | `is not` | text, numeric, date, choice |
| `>` | `is more than` | numeric, date |
| `<` | `is less than` | numeric, date |
| `>=` | `is at least` | numeric, date |
| `<=` | `is at most` | numeric, date |
| `selected` | `includes` | choice |
| `selected-not` | `does not include` | choice |
| `not` | `is not selected` | text, numeric, date, choice, geo |
| `ref` | `has an answer` | text, numeric, date, choice, geo |
| `today` | `today` | *(field-independent — see note)* |

`not` and `ref` are deliberately **broad and identical** — the answered/negation check applies to ANY answerable field (text, numeric, date, choice, geo). (`not` was previously mis-listed as `choice, text, unknown`, which wrongly de-emphasized real numeric/date/geo fields; it is now corrected to the full answerable set, matching the `ref` contract and §6's broadness test.) `today` takes no field operand, so when `op==='today'` the field `<select>` is `disabled` (already the case via `COND_OPS_NEED_FIELD` at `FormEditor.tsx:1112-1114`, which excludes `today`); no filtering applies.

**`inferFieldKind(type: string)` → kind, EXHAUSTIVE over `QUESTION_TYPES` (`types.ts:104-122`) + `STRUCTURAL_TYPES` + compound + unknown:**

| `SurveyRow.type` | `FieldKind` |
|---|---|
| `text`, `string`, `barcode`, `note`, `hidden` | `text` |
| `integer`, `decimal` | `numeric` |
| `date`, `time`, `dateTime` | `date` |
| `select_one`, `select_multiple`, **`select_one X`**, **`select_multiple X`** (suffix regex, reuse `:1094`) | `choice` |
| `geopoint` | `geo` |
| `calculate` | `unknown` (no inherent type — always-pass; choice-upgraded by `fieldChoices`, see §5) |
| `image`, `audio`, `video` | `unknown` (no comparison semantics — always-pass, not de-emphasized) |
| `begin group`/`end group`/`begin repeat`/`end repeat` | never reach the builder (`isStructural` filter at `:522`/`:549`); classifier returns `unknown` defensively |
| any unrecognized / contact-injected (no type) / empty | `unknown` (explicit fall-through — Lorena's "no wrong bucket" gate) |

A new `QUESTION_TYPES` entry that the switch doesn't handle falls through to `unknown` (safe) AND must be caught by the exhaustiveness test (§6).

### 3. Behavior spec

**Two-way soft narrowing (both derive from `OP_FIELD_KINDS`):**

- **Op-first (op narrows fields):** when `state.draft.op` is set, compute `typicalKinds = OP_FIELD_KINDS[op]`. For each field in `props.fieldOptions`, look up `kind = fieldKinds[name]` (see plumbing in §5; `unknown` if absent). A field is **typical** iff `kind === 'unknown'` OR `!typicalKinds.includes(kind)` is false — i.e. typical iff `kind === 'unknown'` OR `typicalKinds.includes(kind)`. Any kind absent from `typicalKinds` and not equal to `'unknown'` is the only "atypical" case; any kind that is simply not enumerated anywhere (future enum growth) is treated as `unknown` and passes. Typical fields render in the "Typical for this check" `<optgroup>` first; non-typical fields render in the "Other fields" `<optgroup>` below, still selectable. The selected/rehydrated field always renders. `choice` fields are detected via `kind==='choice'` (covers contact-injected selects through the §5 `fieldChoices` upgrade).
- **Field-first (field narrows operators):** when `state.draft.field` is set and `state.draft.op` is empty/being chosen, compute `kind = fieldKinds[field]`. Ops where `OP_FIELD_KINDS[op].includes(kind) || kind==='unknown'` (or `kind` absent from every list) are "typical" and grouped first in the op `<select>` via `<optgroup>`; the rest stay reachable in an "Other operators" `<optgroup>`. `unknown`/`${ref}` fields surface ALL ops.
- Both directions are **reversible and non-trapping by construction.** The op-list narrowing is **ORDERING/grouping-only via `<optgroup>`** — all 11 ops are ALWAYS in the DOM and selectable, so it needs no escape-hatch toggle; **a custom listbox for the op picker is explicitly forbidden** (it could hide options). The field list gets the explicit "Show all fields" toggle. Picking a field the current op would de-emphasize auto-relaxes the field list to show-all so an existing selection is never stranded.

**"Show all fields" toggle:** a persistent, discoverable control rendered adjacent to the field `<select>` whenever filtering is active (not hover-only, not in a menu). Local UI state (`const [showAllFields, setShowAllFields] = useState(false)`), NOT reducer state — it must never enter `BuilderState` or any serialization path. When on, no grouping/de-emphasis is applied (flat list). If the rehydrated/selected field is non-typical for the current op, the toggle defaults to **on** so the saved field is visible immediately. (There is intentionally NO `showAllOps` toggle: the op list is `<optgroup>`-grouped only and never hides an op.)

**Accessible compatibility marker:** delivered as the `<optgroup label="…">` section heading on both the field and op pickers ("Typical for this check" / "Other fields"; "Common operators" / "Other operators"). The heading text carries the meaning, is greyscale-legible, and is never color-only. No per-`<option>` `title`/`aria-label` badge is claimed for the native-`<select>` path (those are not reliably surfaced and cannot nest markup); the `<optgroup>` heading is the affordance.

**Contact-injected fields (no type):** classified `unknown` → always-pass under every op, never de-emphasized, never hidden. **Choice-upgrade (single signal):** if `fieldChoices[name]` is non-empty (Slice 1 signal), the field's kind is upgraded to `choice` regardless of `inferFieldKind(row.type)` (see §5), so `selected`/`selected-not` keep it typical and the value cell renders the dropdown; otherwise it falls back to the existing free-text `<input>` (`:1364-1387`) unchanged. **Scope note:** this addendum's contact-field handling applies to contact fields that ALSO appear as survey rows — the real-world CHT `inputs/contact/*` injection pattern, which the fixture models (`sex` is a survey `calculate` row fed from `../inputs/contact/sex`). Contact fields that exist ONLY in `fieldChoices` with no corresponding survey row are NOT in `fieldOptions` (`earlierFields` is built from `form.survey` only at `:520-523`/`:547-550`) and therefore are not selectable in the field `<select>` — that is existing pre-addendum behavior, left unchanged to honor the no-signature-break gate, and documented as a known limitation (§7). Documented known limitation: contact text/integer/date fields stay `unknown` (free-text, all ops) this release — no server `contactFieldTypes` scan (deferred, §7).

**Raw / self-check consistency (Lorena gate):** the visual builder only ever writes a canonical `ClauseOp`; the raw-text editor still accepts any hand-typed op. If a user hand-edits raw text using an op the filter would de-emphasize, the existing §3.1 parse→reserialize self-check routes anything outside the grammar to `isRawFallback` — so the filter (a view) never causes a silent op mismatch. Filtering adds NO new save behavior.

### 4. Natural-language label table (DISPLAY-ONLY — serialized XPath unchanged)

Replace the raw symbol / XPath-leaking option labels at `FormEditor.tsx:1344-1362`. The `<option value=…>` (the canonical `ClauseOp`) is **unchanged**; only the visible text changes. Where an op also has a `COMPARISON_PROSE` entry, the dropdown MUST read from that same const so the dropdown and the `This row shows when:` preview never drift (single microcopy source — Lal/Developer must-have).

| `ClauseOp` value (unchanged) | Current label (`:1345-1361`) | New friendly label | Source const |
|---|---|---|---|
| `=` | `= value` | **`equals value`** (`is` in preview) | `COMPARISON_PROSE['=']` = `is` |
| `!=` | `≠ value` | **`is not value`** | `COMPARISON_PROSE['!=']` = `is not` |
| `>` | `> value` | **`is more than value`** | `COMPARISON_PROSE['>']` |
| `<` | `< value` | **`is less than value`** | `COMPARISON_PROSE['<']` |
| `>=` | `≥ value` | **`is at least value`** | `COMPARISON_PROSE['>=']` |
| `<=` | `≤ value` | **`is at most value`** | `COMPARISON_PROSE['<=']` |
| `selected` | `includes value` | **`includes value`** (keep) | matches `clauseToProse` `includes` |
| `selected-not` | `does not include value` | **`does not include value`** (keep) | matches `clauseToProse` |
| `not` | `not(field)` ← XPath leak | **`is not selected`** | new entry (see note) |
| `ref` | `just ${field}` ← XPath leak | **`has an answer`** | new entry |
| `today` | `today()` ← XPath leak | **`today`** | new entry |

Implementation: extend with `OPERATOR_LABELS: Record<ClauseOp, string>` derived from `COMPARISON_PROSE` for the six comparisons plus the five non-comparison entries, OR keep the comparison labels reading `COMPARISON_PROSE` directly and add literal strings for `selected`/`selected-not`/`not`/`ref`/`today`. Either way ONE shared vocabulary. The optgroup category labels (`comparison`/`select_multiple`/`logic`/`reference`) may stay or be relabeled to plain language (`Compare a value`/`Multi-select`/`Other`); category labels are cosmetic and serialization-irrelevant. **No XPath token (`selected(`, `not(`, `today()`, `${field}`, `div`, `floor`) may appear in any option the user picks.** `today()` may remain a code chip in the PREVIEW per v0.2 §9, but the DROPDOWN option is `today`.

Serialization invariant (proven, not asserted): `clauseToRule`/`serializeAnyParsed` read `Clause.op` (the option `value`), never the label text. Changing any label cannot move a byte of XPath. Guarded by the §6 relabel test.

### 5. File-by-file deltas (REAL anchors)

**`shared/src/xlsform/types.ts`** (modify) — add the classifier next to `isStructural` (`:131-134`).
- Factor the suffix regex into an exported helper, e.g. `export const SELECT_TYPE_RE = /^(select_one|select_multiple)\s+(\S+)/i;` (then `buildFieldChoices:1094` imports it instead of re-declaring — kills the duplicate).
- `export type FieldKind = 'text' | 'numeric' | 'date' | 'choice' | 'geo' | 'unknown';`
- `export function inferFieldKind(type: string): FieldKind` per the §2 table (trim+lowercase like `isStructural`; check `SELECT_TYPE_RE` and bare `select_one`/`select_multiple` → `choice`; explicit `default: unknown`). **~25 LOC.** Re-exported automatically via `shared/src/index.ts`. NOTE: lands in `shared` but is consumed only client-side; it touches NO parser/serializer — needs its own `node --test` case (§6).

**`shared/src/conditionBuilder/conditionReducer.ts`** (modify — ADD const only, no logic change) — beside `ClauseOp` (`:44-55`).
- `export const OP_FIELD_KINDS: Record<ClauseOp, FieldKind[]> = { … }` per §2 (with `not` and `ref` both = `['text','numeric','date','choice','geo']`). Import `FieldKind` from `../xlsform/types.js`.
- Add derived helpers as PURE selectors (`unknown` short-circuits; **a kind absent from the op's list is treated as always-pass**, identical to `unknown`):
  - `export function fieldsTypicalForOp(op: ClauseOp, kind: FieldKind): boolean` → `kind === 'unknown' || OP_FIELD_KINDS[op].includes(kind)` returns `true`; for a kind not in the list AND not `'unknown'`, returns `false` only if the kind is one of the enumerated atypical kinds — implemented as: typical iff `kind === 'unknown' || OP_FIELD_KINDS[op].includes(kind) || !ALL_LISTED_KINDS.has(kind)` where `ALL_LISTED_KINDS` is the union of every kind appearing in any `OP_FIELD_KINDS` value. This guarantees a future unlisted kind passes.
  - `export function opsTypicalForKind(kind: FieldKind): ClauseOp[]` (reads `OP_FIELD_KINDS`; `unknown` / unlisted kind → all 11 ops).
- **DO NOT** wire these into `clauseToRule`/`serializeBuilderState`/`serializeAnyParsed` — they are pure selectors for the UI. **~40 LOC.**

**`client/src/ui/FormEditor.tsx`** (modify).
- **Build a `fieldKinds` map** mirroring `fieldChoices`. In the `useMemo` that builds `fieldChoices` (the `buildFieldChoices` consumer), also build `fieldKinds: Record<string,FieldKind>` from `form.survey` rows with the choice-upgrade rule: `fieldKinds[name] = (fieldChoices[name]?.length ? 'choice' : inferFieldKind(row.type))`. This makes contact-injected `select`s (e.g. `sex`, a `calculate` row that carries `fieldChoices['sex']`) classify as `choice`, satisfying the §3/§6 "select narrows to choice incl. sex" gate; without the upgrade `sex` would be `unknown` and the e2e would fail. Also seed `choice` entries for any name present in `fieldChoices` but absent from `form.survey` (defensive; such names are not in `fieldOptions` so they won't render, but the map stays consistent). Names absent from both → treated `unknown` at lookup (always-pass). This avoids any change to the `fieldOptions: string[]` signature or the `earlierFields.map(r=>r.name)` plumbing at `:520-523`/`:547-550` — Lorena's "no signature break across consumers" gate. Pass `fieldKinds` down through `SurveyRowCard` props (`:679`) → the `<UnifiedConditionBuilder>` invocation (`:789-794`) → `UnifiedConditionBuilder` props (`:1178-1179`), exactly parallel to `fieldChoices`.
- **Op-dropdown labels** (`:1344-1362`): replace the option text per §4, reading `COMPARISON_PROSE`/`OPERATOR_LABELS`. Option `value`s unchanged. Keep all 11 ops in the DOM; group via `<optgroup>` only — no custom listbox.
- **Field `<select>`** (`:1331-1335`): replace the flat `props.fieldOptions.map` with a typical/atypical split using `fieldsTypicalForOp(state.draft.op, fieldKinds[n] ?? 'unknown')`. Render via two `<optgroup>`s ("Typical for this check" / "Other fields") when an op is set and `!showAllFields`; flat when `showAllFields` or no op chosen. The selected field always renders regardless of bucket. Add the "Show all fields" toggle adjacent.
- **Op `<select>`** field-first ordering (`:1337-1363`): when `state.draft.field` set, group ops via `opsTypicalForKind(fieldKinds[field] ?? 'unknown')` into "Common operators" / "Other operators" `<optgroup>`s; all 11 ops stay in the DOM and reachable. No `showAllOps` toggle needed (grouping-only).
- Local `useState` for `showAllFields` (NOT reducer state). No change to `setDraft`, `serializeBuilderState`, or any write path. **~70 LOC.**

**`client/src/state/store.ts`** — **NO change** (no new payload field; `fieldKinds` is derived client-side from `form.survey` + `fieldChoices`, both already in the store).

**`server/src/routes/project.ts`** — **NO change.** The `contactFieldTypes` EXTENDED scan is explicitly **deferred** (§7); the MINIMAL `unknown`→always-pass + `fieldChoices` choice-upgrade path needs no server work. (All four personas accept deferral.)

### 6. Test additions

**Unit — `shared/src/xlsform/types.test.ts`** (new or extend) for `inferFieldKind`:
- **EXHAUSTIVE over `QUESTION_TYPES`:** iterate `QUESTION_TYPES` (`types.ts:104-122`) and assert `inferFieldKind(t)` returns a `FieldKind` that is one of the SIX enum members (`text|numeric|date|choice|geo|unknown`) — never `undefined` and never an out-of-enum value. Pin the co-domain: assert the returned value is a member of a literal `['text','numeric','date','choice','geo','unknown']` set, so adding a 7th `FieldKind` without updating the classifier+map fails here. Assert each `QUESTION_TYPES` member maps to its §2 bucket explicitly.
- Compound: `inferFieldKind('select_one sex') === 'choice'`, `'select_multiple symptoms' === 'choice'`.
- Fall-through: `inferFieldKind('weird_custom_type') === 'unknown'`, `inferFieldKind('') === 'unknown'`, `inferFieldKind('calculate') === 'unknown'`.
- Structural strings → `unknown` (defensive). **~45 LOC.**

**Unit — `shared/src/conditionBuilder/conditionReducer.test.ts`** (extend) for the map:
- **`OP_FIELD_KINDS` completeness pinned to exactly 11:** iterate a literal list of the 11 `ClauseOp` values (`['=','!=','>','<','>=','<=','selected','selected-not','not','ref','today']`); assert the list length is `11` and that each has a defined array entry — guards the `Record<ClauseOp,...>` completeness AND fails if a future op is added (or the count drifts).
- `fieldsTypicalForOp(op, 'unknown') === true` for ALL 11 ops (always-pass contract).
- **Always-pass-for-unlisted-kind contract:** for EVERY `FieldKind` value NOT listed in ANY `OP_FIELD_KINDS` entry (compute the complement of the union of all listed kinds; today this is empty besides `unknown`, but the test must also pass a synthetic/cast unlisted kind), assert `fieldsTypicalForOp(op, kind) === true` for all ops. This protects the never-de-emphasize contract against future `FieldKind` enum growth, not just future `QUESTION_TYPES` growth.
- `selected`/`selected-not` typical only for `choice` (and `unknown`); not for `text`/`numeric`/`date`/`geo`.
- `>`/`<`/`>=`/`<=` typical for `numeric`/`date`/`unknown`, not `text`/`choice`/`geo`.
- **`not` and `ref` broadness:** assert `fieldsTypicalForOp('not', k)` AND `fieldsTypicalForOp('ref', k)` are `true` for `k` in `numeric`, `date`, `geo`, `text`, `choice` (mirrors the §2 broad contract; catches the old `not`-too-narrow bug).
- `opsTypicalForKind('date')` includes the comparison ops; `opsTypicalForKind('unknown')` returns all 11. **~55 LOC.**

**Unit — relabel round-trip guard** (extend `conditionReducer.test.ts`): build a `BuilderState` with each op, run `serializeBuilderState`/`serializeAnyParsed`, assert the produced XPath uses canonical tokens (`=`, `selected(`, `not(`, `${…}`, `today()`) and is **identical regardless of label const values** — i.e. assert `Clause.op` is one of the 11 canonical values and that no label string appears in the serialized output. The existing `relevantParser.chain.roundtrip.test.ts` and `conditionReducer.test.ts` MUST stay green untouched (they make no label assumptions).

**Fixture extension** (`client/tests/fixtures/mini-config`): the committed `forms/app/pregnancy.xlsx` already carries a `date` field (`lmp_date`), the contact-injected `sex` (a `calculate` row fed from `../inputs/contact/sex`, upgraded to `choice` via `fieldChoices`), and a `select_multiple danger_signs`. **Add at least one `integer`/`text` field** alongside these so the date-/numeric-check e2e has a clearly-atypical field to push to "Other fields". Regenerate via `client/tests/fixtures/build-mini-config.mjs` (already modified per git status) — add the row there so it stays reproducible on a fresh clone.

**e2e — `client/tests/condition-builder.spec.ts`** (extend; mini-config fixture, no env export):
- **Op-first date filtering:** pick the `is more than` op → assert a date/numeric field appears in the "Typical for this check" group and a text/name field appears under "Other fields", and that the text field is STILL selectable (selecting it succeeds).
- **Field-first narrowing:** pick the `lmp_date` date field → assert comparison ops are grouped under "Common operators"; assert all 11 ops remain in the DOM.
- **Show-all escape hatch (field list only):** with a filter active, click "Show all fields" → assert previously-deprioritized fields render in the flat list. (No op-list toggle is expected; op grouping is non-hiding.)
- **select op narrows to choice:** pick `includes` → field list "Typical" group contains the `choice` fields incl. contact-injected `sex` (verifying the §5 choice-upgrade) and `danger_signs`; non-choice fields still reachable under "Other fields".
- **Unknown always-pass:** a `calculate` field with NO choices (e.g. `_id`) stays selectable under a numeric-only op like `>` (assert present in the field list, in either group).
- **Relabeled ops, byte-identical save:** build `${sex} = 'female' and ${lmp_date} != ''`-style expression via the relabeled dropdown (`equals`, etc.), save, read `row.extras['relevant']` via API → assert canonical XPath identical to the v0.2 happy-path expectation (no label leakage). **~95 LOC.**

**Gates:** all existing shared + e2e suites stay green (the shared suite is run via `pnpm --filter @cht-ui/shared test`; cite the live total from that run rather than a hard-coded count — the prior "85/104" figures were stale, the tree currently has ~131 `test()` calls across 11 `*.test.ts` files); `pnpm typecheck` clean on all `ParsedExpression` consumers (proves nothing leaked into the parser); smoke `Round-trip stable: YES` on the fixture; no new lint warnings on changed files.

### 7. Persona acceptance gates

**Bhishan (PO/PM):**
- Soft-filter only: every field in `fieldOptions` stays selectable; "Show all fields" always one click away; a field he knows exists is findable in seconds.
- No XPath in any dropdown he touches (`not(`, `today()`, `${field}`, `div` all gone → `is not selected`, `today`, `has an answer`).
- Prose preview reflects the relabeled/filtered picks live as sentences (comparisons + `selected()`/`not()`), unchanged from v0.2 §9.
- Filtering never changes what gets saved.

**Lal (Designer):**
- SOFT-filter + persistent discoverable "Show all fields" escape hatch (field list) + auto-relax when a selection would be hidden; op list is grouping-only and never hides an op; never hard-hide.
- Marker is the `<optgroup>` section-heading text (greyscale-legible, never color-only); single binary state; no undeliverable per-`<option>` badge claim.
- Dropdown labels are plain language from the SAME microcopy source as the preview (`COMPARISON_PROSE`) — zero drift; no cute labels; no XPath in the selectable option.
- Two-way narrowing reversible/non-trapping: field list via toggle, op list via non-hiding `<optgroup>`.
- `${other_field}` + untypeable contact fields = `unknown` = fully permissive.

**Lorena (QA):**
- `inferFieldKind` + `OP_FIELD_KINDS` are single-sourced pure exports with EXHAUSTIVE unit tests over `QUESTION_TYPES` (+ compound + unknown fall-through) and co-domain pinning; a new unmapped type/kind fails the test, never silently mis-buckets.
- `unknown`/compound/contact bucket → show-all, asserted explicitly; unlisted-`FieldKind` always-pass asserted.
- `not`/`ref` broadness over all answerable kinds asserted (catches the old `not` mis-bucket).
- Soft-filter e2e proves the field escape hatch reveals deprioritized fields; both narrowing directions tested; `selected`-narrows-to-choice (incl. contact `sex`) tested via the `fieldChoices` choice-upgrade.
- Relabel guard test proves byte-identical serialized XPath; existing shared + e2e suites stay green (live count cited from `pnpm --filter @cht-ui/shared test`, not a stale hard number); typecheck clean on all consumers.
- Contact-field typing GUESSED nowhere (no server scan); `choice` comes only from the existing `fieldChoices` signal; documented `unknown`→show-all limitation + contact-only-not-injected fields out of `fieldOptions` by existing design.
- Fixture carries a date + text/integer field; e2e fresh-clone runnable, no `W:\` dependency, no env export.

**Developer (correctness owner):**
- One `OP_FIELD_KINDS: Record<ClauseOp, FieldKind[]>` keyed by the real 11-value union; field-first DERIVED from it (no parallel table); `not`/`ref` broad and consistent with their prose.
- One `inferFieldKind` reusing the factored-out `SELECT_TYPE_RE` (no third regex copy); `unknown` AND any unlisted kind are first-class always-pass (de-emphasis predicate keyed to "absent from op's list", not the literal `'unknown'`).
- Filtering mutates the option LIST/grouping only — never `state.draft.field`/`op`; rehydration bypasses the filter; selected field always renders; op picker is native `<select>` + `<optgroup>` (no hiding listbox).
- `fieldKinds` derived client-side from `form.survey` + `fieldChoices` (choice-upgrade), no `fieldOptions`/`earlierFields` signature change.
- Zero change to `parseRelevant`/`serializeRelevant`/`parseRelevantGrouped`/`serializeAnyParsed` signatures or `ClauseOp` semantics; labels never enter `clauseToRule`. `inferFieldKind` lives in `shared/types.ts` (own `node --test`) but touches no parser; `OP_FIELD_KINDS` + selectors in the reducer module as pure UI helpers.

### Version bump

**v0.2 → v0.3.** Adds two UI-only Slice-2 features: (1) type-aware **soft** field/op filtering driven by a single shared `inferFieldKind` + `OP_FIELD_KINDS` map (always-pass `unknown` AND any unlisted-kind bucket, `fieldChoices` choice-upgrade for contact selects, persistent "Show all fields" escape hatch on the field list, non-hiding `<optgroup>` grouping on the op list, accessible `<optgroup>`-heading marker, never hard-hide, never mutates draft or save); (2) natural-language operator-dropdown labels sourced from the existing `COMPARISON_PROSE`/`CONNECTOR_LABELS` consts (display-only, serialized XPath byte-unchanged). No parser/serializer/server changes; `contactFieldTypes` server scan and date-op/numeric-vs-string sub-typing explicitly deferred. Anchors corrected to the post-Slice-2 tree (incl. `<UnifiedConditionBuilder>` at `:789-794`, not the `1034-1050` `RelevantRuleBuilder` range).
