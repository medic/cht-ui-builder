node_modules/
dist/
build/
.turbo/
*.log
.DS_Store
.env
.env.local
coverage/
.vite/
test-results/
playwright-report/
# Demo artifacts (storyboard PNGs) regenerated by poc-build.spec.ts.
client/demo/
# Per-machine Claude Code permission allowlist; the project-scoped
# .claude/settings.json IS checked in.
.claude/settings.local.json
.playwright-mcp/
# Per-machine throwaway Vite config for stable local runs (ignores
# shared/dist in the watcher to avoid HMR storms). Not meant to be committed.
client/vite.config.run.mjs
# Planning docs: internal design conversations, not repo documentation.
# They go stale the moment the work ships and describe decisions that were
# often reversed during the build. Kept locally; what shipped is documented in
# docs/deployment-requirements.md and the other docs/*.md.
docs/plans/
